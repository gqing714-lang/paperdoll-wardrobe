# 纸娃娃素材图包制作说明

这份说明供整理图包的人使用。只负责画素材的使用者不需要写 JSON：提供图片、部件名称、所属栏目和染色区域名称即可。

## 最简单的工作流程

1. 所有图片保持与纸娃娃相同的完整画布，当前标准为 `286×222`。
2. 图片之间不得移动坐标，也不要裁掉透明边缘。
3. 普通部件只需要一张前片 PNG。
4. 上衣、下装、连衣裙和外套可以额外提供一张后片 PNG；后发可以提供底层 PNG；左右辫只需要一张主图。
5. 需要分区染色时，每个染色区域额外提供透明蒙版 PNG：需要染色的位置为白色不透明，其他位置完全透明。
6. 将图片与 `pack.json` 一起压缩成 ZIP，然后在扩展中选择“导入图包”。

## 图包结构示例

```text
青的白衬衫.zip
├─ pack.json
└─ shirt/
   ├─ front.png
   ├─ back.png
   ├─ dye_main_front.png
   ├─ dye_main_back.png
   └─ dye_collar_front.png
```

ZIP 外面多套一层同名文件夹也可以，但 `pack.json` 必须与图片的相对路径对应。

## pack.json 示例

```json
{
  "format": "st-paperdoll-pack",
  "formatVersion": 1,
  "id": "qing-white-shirt",
  "name": "青的白衬衫",
  "version": "1.0.0",
  "author": "青",
  "canvas": {
    "width": 286,
    "height": 222
  },
  "items": [
    {
      "id": "white-shirt",
      "name": "白衬衫",
      "slot": "top",
      "front": "shirt/front.png",
      "back": "shirt/back.png",
      "dyeRegions": [
        {
          "id": "main",
          "name": "主布料",
          "defaultColor": "#eeeeee",
          "tintStrength": 100,
          "saturation": 100,
          "brightness": 100,
          "contrast": 100,
          "preserveLines": true,
          "frontMask": "shirt/dye_main_front.png",
          "backMask": "shirt/dye_main_back.png"
        },
        {
          "id": "collar",
          "name": "领口",
          "defaultColor": "#ffffff",
          "frontMask": "shirt/dye_collar_front.png"
        }
      ]
    }
  ]
}
```

## 栏目名称

`slot` 使用以下内部名称：

| 中文栏目 | slot |
| --- | --- |
| 人模 | `body` |
| 后发 | `back_hair` |
| 左辫 | `braid_left` |
| 右辫 | `braid_right` |
| 鬓发 | `side_hair` |
| 前发 | `front_hair` |
| 眼型／眼白 | `eye_shape` |
| 瞳孔 | `pupil` |
| 眼睛高光 | `eye_highlight` |
| 眼皮 | `eyelid` |
| 眉毛 | `eyebrow` |
| 嘴巴 | `mouth` |
| 面饰 | `face_mark` |
| 面部底效 | `face_effect_base` |
| 面部顶效 | `face_effect_top` |
| 内裤 | `panty` |
| 内衣 | `underwear` |
| 手饰 | `wrist_accessory` |
| 袜子 | `socks` |
| 鞋子 | `shoes` |
| 下装 | `bottom` |
| 上衣 | `top` |
| 连衣裙 | `dress` |
| 外套 | `coat` |
| 背景配饰-2 | `accessory_back_2` |
| 背景配饰-1 | `accessory_back_1` |
| 颈饰 | `neck_accessory` |
| 后发饰 | `hair_deco` |
| 兽耳 | `head_ears` |
| 通用配饰 | `accessory1` |

当前只有 `top`、`bottom`、`dress`、`coat`、`back_hair` 可以填写 `back`。左右辫和其他栏目都按单图处理。

## 前后片与发型前／底层规则

- 两张图片必须属于同一个 `items` 条目，不能拆成两个可选部件。
- 后片是可选的；没有后片时省略 `back` 字段。
- `top`、`bottom`、`dress`、`coat`：`front` 是服装前片，`back` 是服装后片，两张同时显示。
- `back_hair`：`front` 是“后发（前）”，`back` 是“后发（底）”，两张分别固定在人模前后并同时显示。
- `braid_left`、`braid_right`：只填写 `front` 主图。扩展按该辫子的放置设置，把同一张主图切换到人物前层或后层；不要填写 `back`。
- 切换服装或后发时只读取新部件自己的第二张图；新部件没有时会清空隐藏底层，不继承上一件素材。辫子始终只使用当前部件的主图。
- 上衣／下装遮挡顺序切换时，前片与后片会一起交换。
- 服装或后发同一染色区的前片蒙版和后片蒙版使用同一个区域 `id`，用户只需要调整一次完整的高级染色参数。辫子只填写 `frontMask`，它会随主图一起切换层级。

## 分区蒙版规则

- 蒙版必须是透明 PNG。
- 染色范围使用白色不透明像素；不染色范围完全透明。
- 蒙版、前片、后片必须与图包 `canvas` 尺寸完全一致。
- 蒙版不能裁紧，不能缩放，不能移动。
- `defaultColor` 只用于界面初始色提示。用户未改色时直接显示原始图片，不会强制重新染色。
- `tintStrength`、`saturation`、`brightness`、`contrast` 和 `preserveLines` 是可选初始值；省略时依次使用 `100、100、100、100、true`。
- 在扩展的高级染色模块选择“整体”或具体区域，即可独立调整颜色、染色强度、饱和、明度、对比和保留暗线。
- 为了获得干净的换色效果，可染色布料建议使用白色或中性灰原稿，并保留原本的明暗和褶皱。
- AI 剧情配色会直接把 HEX 用 Overlay（覆盖）方式混合到原图；它只认识此处 `dyeRegions` 声明的区域 `name`，并用对应前片／后片蒙版限制颜色范围。没有区域声明时使用“整体”。手动高级染色仍按原来的滑杆算法工作。
- 区域名称应唯一，避免使用“整体”作为局部区域名称；蒙版透明处不染色。不打算自动染色的饰件不应放进该区域蒙版。

## 更新图包

- `id` 相同表示同一个图包。
- 增加或替换图片时保留图包 `id` 和部件 `id`，提高 `version` 后重新导入。
- 扩展会提供“更新现有图包”和“作为副本导入”。
- 更新会保留仍然存在的稳定部件编号、方案引用、模型识别名和服装池选择。

## 限制

- ZIP 压缩包最大 80MB，解压后最大 180MB。
- 单张图片最大 24MB。
- 单包最多 200 个逻辑部件，每个部件最多 12 个染色区域。
- 图包编号、部件编号和染色区编号使用英文字母、数字、点、短横线或下划线；给用户看的 `name` 可以使用中文。
