# CHANGELOG

## 0.2.32（内置素材 0.1.1）
- 从 Google Drive 的「纸娃娃/发型/前发/汪汪狼.png」引入同尺寸透明前发，注册为 `front_hair_008.png`，显示名「汪汪狼」。
- 保留扩展设置抽屉在选项更改、提示词同步等重绘后的展开状态；框架与素材版本分别记录。

## 0.2.31
- 修正 AI 剧情款使用纯白时亮部发白、使用纯黑时暗部细节变平的问题：仍以 Overlay 混合为基础，极端色按明度保留一部分原图灰度。
- 移除按单个色道设置 48/255 下限的处理，深棕等暗色不再被强制改成灰色；手动染色及已有配方内容保持不变，图包版本不变。
- 每轮剧情款上限改为四件，修复模型输出上衣、下装、袜子、鞋子四行时鞋子被直接忽略的问题。

## 0.2.30
- AI 剧情款的 HEX 染色改为直接对原图像素执行 Overlay 混合；色道下限 48/255 保留近黑颜色下的褶皱细节。
- 单区使用整体，局部区域按图包名称和蒙版校验并应用；前后片、缩略图与 GIF 共用渲染结果。新配方从原图开始，避免继承旧手动染色使新颜色变脏。
- 移除旧 AI 像素取样、参数搜索和自动校准代码；手动染色与旧衣柜配方保持原有算法。框架升级到 0.2.30，图包版本未更新。

## 0.2.27-local-test
- 将回复末尾状态栏的完整 HTML、全部布局和全部视觉样式迁入内置正则的“替换为”内容；扩展样式文件中不再包含任何状态栏外观规则。
- 状态栏改用正则生成的 `data-*` 标记定位，不再依赖会被 SillyTavern 改名的消息类名；删除脚本中的类名还原与摘要二次加工。
- 扩展脚本只定位最新状态栏的小人槽位，并挂载唯一一份实时纸娃娃渲染结果；历史卡不挂载，重复刷新不会叠加第二层。
- 正则内明确将小人渲染器设为 `filter: none` 与 `box-shadow: none`；用户素材中的脚底椭圆阴影保持逐字节不变。
- 十一栏匹配表达式与 `$1` 至 `$11` 捕获保持不变；结构版本升级为 5，固定 ID、自动同步和用户禁用状态保留逻辑不变。

## 0.2.26-local-test
- 移除状态栏真实纸娃娃渲染器上的 `drop-shadow()`；小人不再按透明轮廓生成偏移剪影。
- 保留用户原素材中的 `shadow_001.png` 脚底椭圆落影及其注册记录；它与整个人形剪影是两种独立效果。
- 正则结构、纸娃娃尺寸、位置、穿戴图层与状态栏布局保持不变；manifest、缓存参数、样式文件名、内置正则描述与文档同步升级为 0.2.26。

## 0.2.25-local-test
- 移除回复末尾状态栏中 `.stpd-inline-doll-box::before` 生成的半透明人形底纹。
- 真实纸娃娃渲染器、尺寸、位置、图层顺序、状态栏骨架和十一栏正则保持不变。
- manifest、主脚本缓存参数、样式文件名、内置正则描述与文档同步升级为 0.2.25。

## 0.2.24-local-test
- 按确认后的 HTML 预览重做回复末尾状态栏布局：摘要保持“小人＋信息”横向排列，完整十一栏改为在下方独占整行展开，不再把左侧小人栏一同拉长。
- 内嵌真实全身纸娃娃由约 `58×102` 放大为约 `90×160`；窄于 350px 时缩至约 `82×146`，继续复用原渲染器、穿戴状态、染色、表情与唯一眨眼运行时。
- 去除旧版大面积规整外框与“当前／历史”角标，改为轻量上下边线、渐隐纹理和 `❃` 展开动效；“全部状态／收拢状态”位于卡片下方。
- 正则的十一栏匹配与 `$1` 至 `$11` 捕获保持不变，只更新替换骨架并把结构版本提升为 4；原固定 ID 会自动同步更新，保留用户的禁用选择。
- manifest、主脚本缓存参数、样式文件名、内置正则描述与文档同步升级为 0.2.24。

## 0.2.23-local-test
- 修复 SillyTavern 消息安全处理会把正则输出的 `stpd-inline-*` 类名改成 `custom-stpd-inline-*`，导致状态栏 CSS 与纸娃娃挂载器同时失联的问题。
- 状态栏每次刷新前只在带 `data-stpd-status-version` 的专属外壳内还原本扩展自己的类名；不扫描或修改其他消息类名，也不增加轮询、观察器或计时器。
- 点击委托改用不会被类名前缀处理影响的 `data-stpd-status-version` 定位；旧消息重新渲染后也能自动恢复横向卡片与最新纸娃娃。
- manifest、主脚本缓存参数、样式文件名、内置正则描述与文档同步升级为 0.2.23。

## 0.2.22-local-test
- 将配套正则同步从纸娃娃主体启动链中完全拆出：manifest 先加载极小的 `bootstrap.js`，同步完成后再以 0.2.22 版本参数加载 `index.js`，避免主体初始化前置错误阻断正则安装，同时绕开旧主脚本缓存。
- 把唯一的规则定义与同步函数收敛到 `status-regex.js`；启动同步、设置面板手动重试及公开检查接口复用同一实现，没有复制规则、增加轮询或叠加生命周期钩子。
- 将正则的十一项命名捕获与 `$<name>` 替换改为 SillyTavern 1.15 可用的数字捕获 `$1` 至 `$11`；固定 ID 与显示名不变，旧规则会原位升级并保留用户禁用状态。
- manifest、脚本、样式文件名和文档同步升级为 0.2.22。

## 0.2.21-local-test
- 修复配套正则成功替换 `<user状态>` 后，状态栏仍以浏览器默认样式纵向裸排的问题。扩展样式文件改用新版本文件名，避免覆盖更新后继续命中 v0.2.18 的旧 CSS 缓存。
- 收紧正则生成的 DOM：移除依赖 CSS 才能隐藏的辅助文字，为纸娃娃槽位增加稳定专属类，并把状态栏结构版本提升为 2；即使 `data-*` 属性被消息清理器处理，扩展仍可通过类名找到槽位。
- 修复“小人完全不出现”的判定错误。只要最新回复含可识别的完整十一栏，该卡片就挂载扩展当前实际纸娃娃；若模型给出的衣物名无法应用，则先恢复真实保存方案，再显示实际造型，不再把整个小人槽位判为无效。
- 新增一个文档级委托点击监听，用于状态栏展开时再做一次幂等挂载；不为每条消息绑定监听，不新增轮询、观察器或眨眼计时器，销毁时会同步移除。
- 状态栏 CSS 去除关键布局对 `color-mix()` 的依赖，保留手机横向结构及夜间主题变量，提高 Android WebView 兼容性。

## 0.2.20-local-test
- 修正 0.2.19 配套状态栏正则未能稳定自动出现的问题。同步方式改为与 Horae 相同的扩展加载入口：直接读取 `extension_settings.regex`，按固定 ID 添加或更新，并调用 `saveSettingsDebounced()`。
- 正则完整内容改为直接内置于 `index.js`；删除外置正则 JSON、正则引擎动态导入、保存接口回退和无效 manifest 生命周期 hooks，只保留一条可追踪的同步路径。
- 同步会清理本扩展自身的同 ID／同名副本、把唯一规则放到列表末尾，并保留用户原有的禁用状态；内容和位置未变化时不重复写入设置。
- 扩展正常加载时即执行同步，不再受内部“启用纸娃娃”开关影响；原有纸娃娃渲染、状态提示词、消息监听、眨眼和销毁逻辑未增加分支或计时器。

## 0.2.19-local-test
- 内置并自动管理 `[纸娃娃] ❃ user状态栏` 全局显示正则；安装、启用和更新时按固定 ID 幂等同步，禁用或清理扩展时只删除该规则，再次启用会自动恢复，不再要求用户手动导入。
- `<user状态>` 统一为头饰、颈饰、内衣、内裤、衣装、外搭、手饰、袜子、鞋子、其他配饰、表情十一栏；衣柜栏目由真实图层键映射，上衣、下装和连衣裙合入衣装，外套归外搭，面饰及零散配饰归其他配饰。
- 新增手机优先的双层折叠状态栏：`❃` 展开横向小人和摘要，内部“详细／收起”控制完整十一栏；正则只影响 Markdown 显示，不改写聊天数据或提示词。
- 最新有效状态卡复用现有纸娃娃渲染器、实时穿戴、图层、整体／分区染色、表情和唯一眨眼计时器；历史卡保留文字，不挂载持续动画，也不以当前穿搭伪装历史快照。
- 补齐消息完成、重新渲染、编辑、候选切换、聊天切换和重载后的幂等重新挂载；没有新增轮询或 MutationObserver，禁用和销毁会移除新增监听、定时刷新与动态挂载。
- 增加启动幂等保护，并在热更新时先调用旧版本销毁函数，避免脚本自启动、SillyTavern `activate` 钩子或版本切换留下重复监听与眨眼计时器。

## 0.2.18-local-test
- 调整剧情配色的默认范围：没有分区蒙版的部件仍使用“整体”；存在真实分区的部件默认只向模型提供并使用这些分区，不再引导模型自动追加整体染色。
- “整体”没有被删除。手动高级染色仍可随时选择整张部件；旧衣柜、旧配方以及模型明确输出的 `整体=#RRGGBB` 继续兼容，主动同时设置整体与分区时仍沿用原叠加顺序。
- 新衣服协议的格式示意改用“染色配方”占位，避免示例本身诱导模型为每件部件都生成整体色；渲染器、配方结构和外部 ZIP 图包格式均未增加新分支。

## 0.2.17-local-test
- 剧情款重新接入原有高级染色引擎：不再为新款写入 `target` 目标色渲染模式，而是把模型给出的成品色自动换算为原有的染色色值、饱和、明度、对比和保留暗线参数。
- 自动校准会读取当前基底的真实像素。内置素材、相册素材及导入后的外部 ZIP 图包使用同一流程，不依赖扩展内预置的素材编号或固定灰度表。
- 外部图包的前片、后片和每个局部蒙版会分别参与取样；同一图包中灰度不同的“外衫／里层”等区域会得到各自的原引擎参数。
- 为深灰底图生成浅色款时，原引擎的明度上限扩展至 400；已有数值和旧衣柜不变，高值仅在素材确实需要补偿时使用。
- 新增异步防串线处理。读取图包像素期间若切换聊天，旧回复不会在新聊天中继续应用即时穿搭。
- v0.2.16 已保存的 `target` 配方继续兼容显示，但新生成剧情款统一保存为 `multiply` 原引擎配方。

## 0.2.16-local-test
- 修正 AI 剧情款在深灰素材上被二次压暗的问题：`<新衣服>` 中的 RGB 现在作为成品主要可见颜色，而不是直接与素材灰度相乘。
- 新增目标色映射。渲染时按每张前片、后片或局部染色蒙版内的实际灰度分布独立取样，以主要亮部校准目标色，并保留原素材的明暗关系与暗线。
- 目标色映射只写入新生成的剧情款；手动调色、已有衣柜条目、旧方案和旧版染色继续沿用原算法，避免升级后外观突变。
- 缓存指纹、衣柜配方、方案快照、即时换装和 GIF 导出均保留目标色模式；整体染色与分区染色使用同一套校准逻辑。
- 动态提示词明确色值代表成品主要可见颜色，无需模型猜测底图灰度或预先提亮色号。

## 0.2.15-local-test
- `<新衣服>` 的标准行格式简化为“区域｜基底｜新名称｜染色配方”，移除容易被模型漏写且没有判定价值的重复 `生成：` 前缀。
- 解析器同时接受无前缀新格式和带 `生成：` 的旧格式，保持 v0.2.13／v0.2.14 提示词与历史回复兼容。
- 四栏数量、单轮件数、真实区域、真实基底、染色范围、色值与同轮名称校验全部保留。

## 0.2.14-local-test
- 修正衣柜增量协议把“范围”写进示例所造成的模型照抄问题；默认合法配方改为单一的 `整体=#RRGGBB`。
- 提示词明确禁止将“范围／染色范围”等说明词作为真实字段；解析器遇到旧错误格式时返回可操作的中文原因。
- 无局部蒙版的基底只能生成一个整体色；使用局部蒙版时必须逐字复制动态清单中的真实染色区名。
- 强化命名边界：允许自然或诗意名称，但不得暗示基底不存在的材质、版型、长度、图案或结构。

## 0.2.13-local-test
- 新增剧情驱动的衣柜配色款：模型可在正文后输出 `<新衣服>`，扩展以当前可用部件为基底创建独立染色配方并保存到当前衣柜。
- 新衣服协议作为独立动态模块附加到现有状态提示词；用户自定义的状态提示词无需改写，可在提示词工作台查看正式注入内容并单独开关。
- 新增 `{{可用染色区}}` 动态变量；只列出当前可用基底真实拥有且名称无歧义的局部蒙版，未列出的基底仍可使用整体染色。
- 严格校验区域、基底、名称、染色范围与六位 RGB 色值；单轮最多处理三件，非法行独立忽略，不改动当前穿搭。
- `<新衣服>` 先于 `<user状态>` 处理；新款可在同一回复的状态块中立即穿上。自动编号或复用旧配色时，会在本轮内部解析模型给出的新名称。
- 相同素材、栏目、语义区域与完整染色配方会复用现有衣柜条目，避免重复回复持续堆积同款；同名不同配方自动编号。
- 剧情生成条目保存原素材引用与染色配方，不复制 PNG；衣柜卡片标记为“剧情款”，GIF 导出自动使用其最终配色。
- 保存剧情款时只持久化衣柜增量；上一轮 AI 的即时穿搭仍保持临时状态，不会借由新增衣服意外写入本地穿搭。

## 0.2.12-local-test
- 新增“一键导出透明特写 GIF”：直接读取当前穿搭、整体／分区染色、表情和真实图层顺序，不依赖屏幕截图。
- 导出范围固定为右侧 160×222 特写，不包含左侧全身小人、预览网格、悬浮位置偏移或界面背景。
- GIF 沿用三帧眼皮，播放“睁眼 → 半闭 → 闭眼 → 半闭 → 睁眼”，随后停约 5 秒并循环。
- 换装、衣柜与表情页的预览下方均可直接导出；设置页保留第二个明确入口。
- GIF 编码器改为点击导出时按需加载，平时不参与渲染、眨眼或常驻监听。
- 内置素材、相册素材与 ZIP 图包均可参与导出；受网站读取限制的网址素材会给出中文提示，不生成错误文件。

## 0.2.11-local-test
- 新增独立衣柜数据层与衣柜页面；原始素材、用户保存的单件衣物和整套穿搭方案不再混为同一概念。
- 原色或完成整体／分区高级调色的部件都可保存到当前衣柜；每个条目独立保存素材引用、整体调色和各染色区完整参数，不复制图片文件。
- 同名条目按“原名、原名01、原名02”自动编号，支持用户改名、复制、覆盖当前配色、另存新款和移除。
- 支持创建、切换、重命名和删除多个衣柜；穿搭方案记录所属衣柜，user 绑定仍引用稳定方案编号。
- 衣柜按九个语义区域分类，并在区域内按真实素材栏目分架展示；通用配饰可手动改为头部、颈部、手部或其他配饰。
- 当前衣柜替代旧服装池成为 AI 可用范围；只注入自定义名称，不发送色号、蒙版或染色参数。
- 默认提示词与 `<user状态>` 改为头部、颈部、贴身、衣装、外搭、手部、腿部、脚部、其他配饰和表情的固定完整结构。
- 新增 `{{当前状态}}`、`{{可用分类部件}}` 变量；旧四变量提示词和旧“衣着：A｜B”回复格式继续兼容。
- 旧服装池自动迁移到默认衣柜；共享配饰别名只迁移一个条目，旧名称、图包稳定编号和染色设置保留。
- AI 即时换装现在会应用衣柜条目的独立配色，并在无效／缺失状态时连同配色一起恢复保存方案；仍不写入回复或本地存档。
- 衣柜在电脑与手机端均保持全身＋特写双预览；分类栏在窄屏横向滚动，素材密度继续支持自动、3、4、5 列。

## 0.2.10-local-test
- 在每次正式生成前监听 `GENERATION_AFTER_COMMANDS` 并强制重新登记实时状态提示词，覆盖普通发送、继续与重新生成等入口；旧版客户端保留 `MESSAGE_SENT` 回退。
- 修复共享配饰在配饰 1／2／3 中被误判为三个同名部件的问题；同一素材现在只占一个 AI 池身份，修改模型识别名时会同步三个共享栏目。
- 真正的不同部件重名不再清空整条提示词；当前状态仍会注入，仅暂停应用有歧义的自动换装结果。
- 提示词编辑器与扩展面板新增真实注入状态、正式内容预览、失败原因和“重新注入并检查”入口；草稿预览与正式注入预览明确分开。
- 后发饰调整到鬓发下层，修复后发饰错误覆盖鬓发的问题；全身、特写与悬浮纸娃娃同步生效。

## 0.2.9-local-test
- 左右辫改为严格单图逻辑：“放到身前／放到身后”只改变同一张主图的渲染层级，不再切换前／底图片。
- 辫子的主图分区蒙版会随图片一起切换层级，前后位置共用同一套完整高级染色参数。
- 旧版双图辫子图包仍可导入；置后图片和置后蒙版会被忽略并在导入检查中提示。
- 隐藏的旧辫子底图选择不再参与渲染，避免素材逻辑与面板说明不一致。
- 图包工坊 v0.1.4 移除左右辫第二张图和第二张蒙版入口，只保留单张主图。

## 0.2.8-local-test
- 修复图包格式遗漏后发前／底层的问题；`back_hair` 的 `front` 固定在人模前，`back` 固定在人模后，两片属于同一逻辑部件。
- 图包左右辫同样支持前／底两张变体，并沿用原有“放到身前／放到身后”二选一逻辑。
- 切换双图发型到单图发型时会主动清空隐藏底层选择，不继承上一件的后发或辫子底层。
- 发型前／底两张图都支持各自的分区蒙版，并共享同一区域的完整高级染色参数。
- 图包工坊 v0.1.3 可直接制作服装前后片、后发前底层和左右辫位置变体。

## 0.2.7-local-test
- 将分区染色并入原有高级染色模块，新增“整体／各染色区”作用范围选择。
- 每个染色区独立支持颜色、染色强度、饱和、明度、对比和保留暗线，并同时作用于对应前片与后片。
- 整体高级调色保留；渲染时先处理整体，再在蒙版范围内叠加分区参数。
- 旧版只保存颜色字符串的分区记录会自动迁移为完整参数对象，旧图包与旧单图素材继续兼容。

## 0.2.6-local-test
- 新增 ZIP 素材图包导入：导入前以中文核对部件数、后片数、染色区和画布尺寸。
- 图包图片改存浏览器独立图片库，普通设置只保存关联信息，避免大量 PNG 撑爆设置容量。
- 同编号图包支持更新或作为副本导入；更新时保留稳定部件编号、方案引用、模型识别名和服装池选择。
- 新增图包管理，可查看版本和部件数并删除单个图包。
- 上衣、下装、连衣裙、外套支持可选后片；前片和后片属于同一逻辑部件。
- 从有后片部件切换到无后片部件时，后片立即清空，不会继承上一件素材。
- 上衣／下装层级切换会同步交换前片和后片，外套固定覆盖连衣裙与普通上下装。
- 图包部件支持多个可命名染色区域；一个颜色同时控制该部件对应的前片和后片蒙版。
- 分区颜色随方案保存；未设置分区颜色时保持原始素材，不强制处理旧素材。
- 保留单张相册／网址导入和全部旧单图素材、方案、user 绑定及 AI 即时换装逻辑。

## 0.2.5-local-test
- 将换装与表情页改为双栏／上下分区的实时工作台，预览与部件列表保持同时可见。
- 手机端使用紧凑双预览和独立滚动素材区；方案与设置页不再保留大预览，减少拥挤。
- 普通换装新增自动、3列、4列、5列密度选择并持久保存。
- 普通部件卡改为紧凑原比例缩略图；服装池管理保留两列完整管理卡。
- 将导入入口移出素材网格，并把调色与特殊层级放到素材列表后方。
- 新增一键脱下服装、清空配饰及全部清空；不会删除素材或覆盖已保存方案。
- 保留 v0.2.4 的浅暖色主题、双预览、提示词预设、剧情即时状态与 user 方案绑定。

## 0.2.4-local-test
- 恢复全身与特写同时展示，删除遮挡画面的预览切换按钮及单预览状态。
- 扩大顶部预览区；窄屏下改为预览在上、角色状态在下，保证两张图完整可见。
- 将深色高对比像素界面改为低对比的浅暖色纸偶工坊界面。
- 保留 v0.2.3 的提示词预设、剧情即时换装、user 方案绑定、服装池与全部素材逻辑。

## 0.2.3-local-test
- 将更衣室重构为深夜像素装备界面，新增单预览状态台和换装／表情／方案／设置四页导航。
- 新增普通换装与服装池编辑模式，减少日常界面的控件密度。
- 新增可编辑状态提示词、四个中文动态变量、格式校验、本轮提示预览和多预设管理。
- 系统默认提示词作为只读基准，自定义预设支持新建、另存、覆盖、删除与切换。
- 保留 v0.2.2 的实时状态优先规则、池外部件限制、左右停靠与正则隐藏方式。

## 0.2.2-local-test
- 新增特写左侧／右侧显示选项，并保存用户选择。
- 精简状态提示词，明确实时状态优先于设定默认衣着，禁止状态块使用可用池之外的部件或表情。
- 脚本改为只解析状态块并换装，不再删除状态块或重新保存 AI 回复；可交由 SillyTavern 正则隐藏。

## 0.2.1-local-test
- AI 回复中的有效状态改为即时覆盖，不再写入聊天元数据、消息扩展字段或本地方案。
- 下一次 AI 回复缺少状态块或状态无效时，自动恢复当前 user 绑定的已保存方案。
- 面板第一屏改为部件换装，状态表情、user 方案和高级设置移至下方。
- 调色控件折叠到当前部件区域，减少首屏占用。
- 黑色底板改为自适应主题的浅色毛玻璃面板，并优化卡片、间距和移动端布局。

## 0.2.0-local-test
- 按新 PSD 逐项替换嘴巴和眨眼，新增眼白、瞳孔与两层面部效果素材。
- 面部底效和面部顶效固定位于完整五官之上，面部顶效覆盖底效。
- 新增可命名的组合状态表情。
- 新增部件模型识别名和服装池。
- 新增 `<user状态>` 提示注入、回复校验、自动换装和状态块移除。
- 当前穿着与表情按聊天存档和 user 分别保存，聊天切换后会重新注入状态提示。
- 删除两个早已不存在的旧轮询计时器清理残留。

## 0.1.14-local-test
- 瘦身减重版。
- 删除预览 contact sheet 和未使用的开发辅助数据文件。
- 将更衣室样式移入 style.css，删除运行时注入样式逻辑。
- 删除入口按钮旧适配残留、旧脚本 enabled 检查残留、未使用的图片尺寸检测函数和无用空函数。
- 不改图层逻辑与素材逻辑。

## 0.1.13-local-test
- 清理入口按钮逻辑，只保留 #options_button 同父级插入。
